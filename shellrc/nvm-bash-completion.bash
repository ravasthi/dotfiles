# --------------------------------------------------------------------------------------------------
# nvm bash completion
# --------------------------------------------------------------------------------------------------

if [[ -r $NVM_DIR/bash_completion ]]; then
  source $NVM_DIR/bash_completion
fi
