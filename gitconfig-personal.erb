[user]
  name = <%= print("Your full name (personal config): "); STDOUT.flush; STDIN.gets.chomp %>
  email = <%= print("Your email (personal config): "); STDOUT.flush; STDIN.gets.chomp %>

[github]
  user = <%= print("Your GitHub username (personal config): "); STDOUT.flush; STDIN.gets.chomp %>
