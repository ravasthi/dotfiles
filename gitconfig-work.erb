[user]
  name = <%= print("Your full name (work config): "); STDOUT.flush; STDIN.gets.chomp %>
  email = <%= print("Your email (work config): "); STDOUT.flush; STDIN.gets.chomp %>

[github]
  user = <%= print("Your GitHub username (work config): "); STDOUT.flush; STDIN.gets.chomp %>
